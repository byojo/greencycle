# 叮当回收 · 上线控制台待办清单（代码外操作）

> 代码侧修复已全部完成（见 `LAUNCH_CHECKLIST.md` 与本次提交）。下面这些事项**只能**在微信公众平台 / 微信云托管控制台手动操作，无法通过代码完成。请按优先级逐项处理。

---

## 🔴 P0 — 上线前必须完成（否则小程序无法正常使用或被拒审）

> **📌 实测结论（2026-08-15 13:42）**：生产域名 `sxyrgy.cn` 的 SSL 证书**已生效**（TrustAsia DV，8/2 签发，10/31 到期），`https://sxyrgy.cn/health` 已返回 200。自定义域名 + SSL 这一项**不用再加了**。当前真正的阻塞是：**代码 push 后自动部署未刷新，线上仍是旧容器**（表现为 `/privacy` 404、CORS 仍 `AllowAllOrigins`）。请先按下方"部署"节手动触发重新部署，再回头登记隐私协议网址。

### 1. ✅ 生产域名 SSL 证书（已生效，无需操作）
- 平台：微信云托管控制台 → 服务 `golang-ox8i` → 「自定义域名」
- 现状：自定义域名 `sxyrgy.cn` 已登记且证书已签发（8/2），HTTPS 访问正常。
- 验证：`curl -I https://sxyrgy.cn/health` → `HTTP 200`，证书校验通过。

### 2. 小程序 request 合法域名（疑似已配置，请确认）
- 平台：微信公众平台 → 开发管理 → 开发设置 → 服务器域名 → request 合法域名
- 确认列表中含：`https://sxyrgy.cn`（可选保留测试域名 `https://golang-ox8i-275614-7-1448098353.sh.run.tcloudbase.com`）。
- 说明：前端 `config.js` 的 `PROD_API` 为 `https://sxyrgy.cn/api/v1`，若该域名已在白名单则真机请求不会报错（你看到的"已经有了"应指此项）。
- 补充：若前端 `wxml` 直接 `<image src="https://...cos...">` 展示 COS 图片，还需在「downloadFile 合法域名」加 COS/CDN 域名。

### 3. 隐私协议登记（合规硬性要求）
- 平台：微信公众平台 → 设置 → 服务内容 → 用户隐私保护指引 / 小程序隐私协议
- 操作：
  1. 填写隐私政策网址：`https://sxyrgy.cn/privacy`（本项目已托管真实隐私页）。
  2. 勾选并说明收集的个人信息类型（手机号、位置信息等）及用途。
- 代码配合：前端 `utils/privacy.js` 已在 `getLocation/chooseLocation` 前调用 `wx.requirePrivacyAuthorize`；`login.js` 已引用该链接。
- 风险：未登记隐私协议，调用位置等接口会被微信拦截，且可能审核不通过。

### 4. 小程序类目与资质
- 平台：微信公众平台 → 设置 → 基本设置 → 服务类目
- 操作：确认已选择「生活服务 / 环保回收」或相关类目，并按要求提交资质（如涉及废旧物资回收经营备案等）。
- 说明：类目不符或资质缺失会被拒审。请核对当前已填类目是否覆盖「旧物回收 / 二手交易 / 积分兑换」业务。

---

## 🟠 P1 — 强烈建议上线前完成（影响体验 / 合规完整度）

### 5. 订阅消息模板核对
- 平台：微信公众平台 → 功能 → 订阅消息 → 我的模板
- 需存在以下两个模板（代码 `order-confirm.js` 与 `exchange.js` 已写死 tmplIds）：
  - 派单通知：`j4dcmYkCBav2QZ8OZQZZjK69Xu4IhUbd-iYt5UG1N-M`
  - 完成通知：`C78o2a0-IRT5hDU520LrS7E29_CXyPLR3YdRa4PI6yI`
- 操作：确认模板 ID 与后台一致、模板关键词齐全；若后台模板被删/改，需同步更新前端 `tmplIds`。
- 现状：`order-confirm.js` 早前已实现授权，`exchange.js` 已补齐，逻辑一致。

### 6. 云托管环境变量核对
- 平台：微信云托管控制台 → 服务 `golang-ox8i` → 环境变量
- 确认以下变量存在且正确（**不要写在代码 / cloudbaserc.json 里**）：
  - `JWT_SECRET`（登录鉴权，17bfc4...）
  - `ADMIN_KEY`（管理后台口令，sxyrkj）
  - `ADMIN_USER_IDS`（1）
  - `WECOM_BOT_KEY`（企业微信群机器人，afdfe1b4-...）
  - `COS_` 前缀（存储桶 `greencycle-image-1255464850`，region `ap-guangzhou`）
- 说明：代码中 `cloudbaserc.json` 仅列变量名，真实值须在控制台设置。

### 7. 使 WECOM_BOT_KEY 等新环境变量生效
- 若近期改过环境变量（如 2026-08-15 更新了 `WECOM_BOT_KEY`），需：
  - 云托管控制台 → 服务 → 「重新部署 / 重启实例」，让新环境变量载入。
- 验证：触发一条回收/兑换工单，确认企业微信群收到通知。

---

## 🟡 P2 — 上线后跟进（非阻塞）

### 8. 客服会话能力（可选增强）
- 当前 `order-detail / order-track / settings` 等页面的"联系客服"已统一改为 `wx.makePhoneCall` 拨打 `15249019944`。
- 若希望用微信原生客服会话（`openCustomerServiceChat`），需在 MP 后台「客服」模块绑定客服账号并配置，再切换前端实现。当前电话方案已可用，无需阻塞。

### 9. 管理后台访问保护
- `/admin` 与 `/privacy` 已静态托管。建议确认 `/admin` 仅管理员可访问（前端校验 `ADMIN_USER_IDS` + `ADMIN_KEY`），避免被爬取。

### 10. 微信开放社区 / 审核自检
- 提交审核前，用「微信开发者工具 → 体验版」在真机走一遍主流程：下单 → 派单 → 取件 → 完成 → 积分；兑换 → 配送 → 送达。
- 确认无"功能开发中"死路（已全部隐藏）、无未授权位置调用、无控制台报错。

---

## 操作顺序建议（实测后修订）
1. **【首要】手动触发云托管重新部署**：控制台 → 服务 `golang-ox8i` → 查看「构建/部署」记录，确认最新一次是否成功；若失败查日志，或直接点「重新部署」让 `main` 的 `cc0e1a8` 上线。部署后验证：`https://sxyrgy.cn/privacy` 返回 200、`/health` 的 CORS 不再回显任意 Origin。
2. 确认 P0 的 2（request 合法域名含 sxyrgy.cn）、4（类目资质）。第 1 项 SSL 已生效，无需操作。
3. 部署成功后，登记 P0 的 3（隐私协议网址 `https://sxyrgy.cn/privacy`，此时页面已 200）。
4. 完成 P1 的 5–7（订阅模板、环境变量、WECOM_BOT_KEY 更新后重启生效）。
5. P2 事项上线后持续跟进。

---
*本清单配合 `LAUNCH_CHECKLIST.md`（代码侧体检与修复记录）使用。代码改动清单见本次 git 提交。*
